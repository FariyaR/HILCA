# Secrets — never commit real keys
.env

# Local databases
*.db

# Python
__pycache__/
*.pyc
.pytest_cache/

# Per-run dialectic artifacts (.md docs + run_log.csv per run)
runs/

# Local tooling / duplicate source material (kept on disk, not in the repo)
.claudeignore
reference/*.docx
